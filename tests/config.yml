---
# Mac App Store installs require an authenticated session, which isn't
# available in CI, causing the playbook to hang. Override to empty list
# so the mas role is skipped entirely.
mas_installed_apps: []
mas_installed_app_ids: []
